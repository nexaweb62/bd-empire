---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const items = [
  { title: 'Les Naufragés de l\'Empire', author: 'A. Moreau', price: 15.90, qty: 1 },
  { title: 'Shadow Blade Vol. 3', author: 'K. Tanaka', price: 9.50, qty: 2 },
];
const sousTotal = items.reduce((sum, i) => sum + i.price * i.qty, 0);
const livraison = 4.90;
const total = sousTotal + livraison;
const fmt = (n: number) => n.toFixed(2).replace('.', ',') + ' €';
---
<BaseLayout title="Panier — BD Empire" description="Votre panier BD Empire.">
<style>
.cart-page{padding:calc(var(--hdr) + 50px) 0 100px}
.cart-layout{display:grid;grid-template-columns:1.6fr 1fr;gap:40px;align-items:start}
.cart-item{display:flex;gap:16px;padding:18px;align-items:center}
.cart-item + .cart-item{border-top:1px solid var(--border)}
.ci-cover{width:64px;height:84px;flex-shrink:0;background:linear-gradient(155deg,var(--panel),hsl(240 10% 12%));border-radius:8px}
.ci-title{font-weight:800;font-size:.95rem}
.ci-author{color:var(--text-muted);font-size:.8rem;margin-top:2px}
.ci-qty{margin-top:8px;font-size:.8rem;color:var(--text-muted)}
.ci-price{margin-left:auto;font-weight:800}
.summary{padding:24px}
.summary h2{font-size:1.2rem;margin-bottom:18px}
.sum-row{display:flex;justify-content:space-between;font-size:.9rem;color:var(--text-muted);margin-bottom:10px}
.sum-total{display:flex;justify-content:space-between;font-weight:900;font-size:1.15rem;margin-top:16px;padding-top:16px;border-top:1px solid var(--border)}
@media(max-width:820px){.cart-layout{grid-template-columns:1fr}}
</style>
<Header />
<main>
  <section class="cart-page">
    <div class="wrap">
      <span class="label">Étape 1 sur 3</span>
      <h1 style="margin-top:14px;font-size:clamp(1.8rem,3.4vw,2.6rem)">Votre panier</h1>

      <div class="cart-layout" style="margin-top:36px">
        <div class="card">
          {items.map((i) => (
            <div class="cart-item">
              <div class="ci-cover"></div>
              <div>
                <p class="ci-title">{i.title}</p>
                <p class="ci-author">{i.author}</p>
                <p class="ci-qty">Quantité : {i.qty}</p>
              </div>
              <span class="ci-price">{fmt(i.price * i.qty)}</span>
            </div>
          ))}
        </div>

        <div class="card summary">
          <h2>Résumé</h2>
          <div class="sum-row"><span>Sous-total</span><span>{fmt(sousTotal)}</span></div>
          <div class="sum-row"><span>Livraison</span><span>{fmt(livraison)}</span></div>
          <div class="sum-total"><span>Total</span><span>{fmt(total)}</span></div>
          <a href="/contact" class="btn" style="width:100%;justify-content:center;margin-top:22px">Passer commande</a>
        </div>
      </div>
    </div>
  </section>
</main>
<Footer />
</BaseLayout>
